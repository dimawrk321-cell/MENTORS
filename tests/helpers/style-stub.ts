// Stylesheet stub: node-environment tests import RSC pages, whose component tree
// pulls in real CSS (katex). Vitest has no PostCSS pipeline — resolve every .css
// import here instead. Styling is verified in the browser, not in unit tests.
const stylesheet = {};
export default stylesheet;
